{
  "name": "prasis",
  "version": "1.0.0",
  "description": "Prasis is a student project developed as part of the frontend curriculum at Noroff.",
  "private": true,
  "scripts": {
    "sass:watch": "sass --no-source-map styles/main.scss styles/main.css --watch",
    "sass:build": "sass --style=compressed styles/main.scss styles/main.css",
    "test": "echo \"No tests yet\""
  },
  "devDependencies": {
    "sass": "^1.93.2"
  }
}
